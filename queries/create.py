import sys
sys.path.append("/workspace/sql-heroes")
from connection import *

# super_vehicles = """ CREATE TABLE IF NOT EXISTS super_vehicles(
#     id int generated always as identity,
#     name varchar,
#     description varchar,
#     hero_id int,
#     PRIMARY KEY(id),
#     FOREIGN KEY (hero_id)
#     REFERENCES heroes(id)
# )
# """

# create(super_vehicles)

